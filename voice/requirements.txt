# Обязательное. Без этого пайплайн не поедет.
# tzdata нужен только там, где нет системной базы часовых поясов: на обычной
# Raspberry Pi OS и Ubuntu она есть, а в тощих контейнерах — нет. Без неё робот
# на «сколько времени в Лондоне» честно молчит, а не врёт на час; но лучше
# пусть отвечает. Пакет крохотный, и ставить его дешевле, чем разбираться,
# почему одна команда из сорока не работает.
tzdata; platform_system != "Windows"
anthropic>=0.69
faster-whisper>=1.0
webrtcvad>=2.0.10
numpy>=1.24
requests>=2.31
websocket-client>=1.7

# Синтез речи ставится отдельно в scripts/setup_voice.sh: колесо piper-tts под
# aarch64 собирается не всегда, и ронять из-за этого всю установку незачем.
# Без Piper робот работает молча — реплики видно субтитрами в пульте.

# Необязательное.
# Яндекс.Музыка. Без неё играет интернет-радио — это рабочий режим, а не
# поломка, поэтому библиотека и не в обязательных. Ставить только вместе с
# активной подпиской Плюс:
# Ставить именно pip'ом из venv: этим же питоном запускается служба, и
# библиотека, поставленная системному, ей не видна.
#     voice/.venv/bin/pip install yandex-music
#     voice/.venv/bin/python voice/yandex_auth.py
