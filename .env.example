# Шаблон секретов. Скопировать в ~/.robot-ai.env и заполнить.
# Сам ~/.robot-ai.env в репозиторий НЕ попадает.

# Ключ. Напрямую — с console.anthropic.com/settings/keys,
# через роутер — ключ роутера.
ANTHROPIC_API_KEY=sk-ant-...

# Адрес API. Пусто — напрямую в api.anthropic.com.
# Через роутер, понимающий протокол Anthropic:
#   ROBOT_API_BASE=https://routerai.ru/api/v1
ROBOT_API_BASE=

# Модель.
#   напрямую:      claude-opus-5 (умнее) | claude-sonnet-5 (дешевле и быстрее)
#   через роутер:  anthropic/claude-sonnet-4.5 | google/gemini-2.5-flash
# Для голоса важна скорость, но инструменты управляют железом — ошибка вызова
# означает «поехал не туда». Модели уровня flash ошибаются на этом заметно чаще.
ROBOT_MODEL=claude-opus-5

# Глубина размышлений: low | medium | high | xhigh | max
# Для разговора low/medium — быстрее отвечает.
# ВАЖНО: при работе через роутер оставить пустым — он этот параметр не знает.
ROBOT_EFFORT=low

# Потолок длины ответа. Для голоса больше и не нужно.
ROBOT_MAX_TOKENS=2048

# Источник аудио и видео.
# phone  — старый Android с приложением IP Webcam (временное решение)
# local  — микрофон, воткнутый в RDK X5 (после приезда ReSpeaker)
ROBOT_AUDIO_SOURCE=phone
# Тот же адрес использует прокси камеры в web/server.py.
ROBOT_PHONE_URL=http://192.168.3.9:8080

# Модель Whisper: tiny | base | small
ROBOT_WHISPER_MODEL=base

# Голос Piper (файл .onnx лежит в voice/models/, в git не коммитится)
ROBOT_PIPER_VOICE=ru_RU-irina-medium

# Куда говорить.
# browser — играет браузер с открытым пультом (пока своего динамика нет)
# local   — сразу в aplay (когда приедет SOTAMIA)
ROBOT_AUDIO_OUT=browser
ROBOT_WEB_URL=http://127.0.0.1:8000

# rosbridge — через него голосовой пайплайн шлёт /cmd_vel
ROBOT_ROSBRIDGE_URL=ws://127.0.0.1:9090

# Home Assistant (опционально, для умного дома)
HA_URL=
HA_TOKEN=
