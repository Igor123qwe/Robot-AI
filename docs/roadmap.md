# План

## v1 — сейчас

- [x] Веб-пульт: джойстики, меканум, батарея
- [x] Автозапуск (systemd): шасси, rosbridge, веб
- [x] Репозиторий и `git pull` вместо `scp`
- [x] Голосовой пайплайн: VAD → Whisper → Claude с инструментами → Piper
- [ ] Заполнить `~/.robot-ai.env` и включить `robot-voice`
- [ ] Проверить, что робот полностью самостоятелен после перезагрузки
- [ ] Huawei AX2 → режим точки доступа (убрать двойной NAT)
- [ ] Купить экран (Waveshare DSI из списка совместимости)

## v1.5 — когда приедет железо

- **ReSpeaker Lite** → аппаратный AEC → barge-in (перебивать робота голосом).
  Тогда же: Silero VAD, переезд на Pipecat или LiveKit Agents,
  openWakeWord вместо проверки слова по тексту.
- **Камера AR0234** → YOLO (person) на BPU, `face_recognition`,
  speaker-ID (SpeechBrain / Resemblyzer)
- **ToF 8×8** → обход препятствий, VFH+ (готовые реализации под ROS 2)
- **Экран** → лицо робота на DSI-панели

## v2 — автономность

- Следование за человеком: `wheeltec_bodyreader` + `simple_follower_ros2`
- Голосовое предупреждение о разряде батареи
- Автовозврат на зарядку (`auto_recharge_ros2`)
- Умный дом: инструменты Claude → Home Assistant
- Датасет `socks_seg_yolov8` с HuggingFace + дообучение — искать носки

## v3 — отложено осознанно

- **Рука-манипулятор:** SO-ARM101 leader+follower + LeRobot + ACT
  (50–100 демонстраций, обучение на внешнем GPU, инференс на RDK X5).
  Альтернатива — myCobot 280 RDK X5.
- **Лидар и SLAM** — пакеты в образе уже есть, но нужен лидар
- **VLA / GR00T** — знать, не трогать
- **Проектор для мультиков на стену** — обсуждали, отложено.
  Энергетика не сходится: батарея 61 Вт·ч, проектор 30 Вт съест её за 2 часа.

## Принципы

- Не писать с нуля то, что уже есть в `~/wheeltec_ros2/src`
- Считать деньги: не покупать то, без чего можно обойтись
- Каждую карточку на AliExpress проверять по полю «бренд», а не по заголовку
