# ТЕХНИЧЕСКИЙ ПЛАН: человечек до 10/10 по всем четырём осям

Все пути от `/home/user/Robot-AI`. Номера строк — по текущему дереву (проверены).

---

## 0. ОБЩИЕ ПРАВИЛА (читать до начала любой работы)

**Р0.1. Файлы проверок — общая зона, но append-only.** `voice/selftest.py` (24344 стр.) и `pc/selftest.py` (2878 стр.) держат список проверок кортежем в `main()` (`voice/selftest.py` — хвост файла, `pc/selftest.py:2859`). Правка перед раздачей волн (10 минут, один человек, до всего остального):

> **Работа 0.** ФАЙЛЫ: `voice/selftest.py`, `pc/selftest.py`. В `main()` после существующего кортежа добавить автодобор: `остальные = [f for имя, f in sorted(globals().items(), key=lambda п: getattr(п[1], "__code__", None) and п[1].__code__.co_firstlineno or 0) if имя.startswith("test_") and f not in набор]` и прогнать их после. Порядок старых не меняется. После этого новая проверка = функция, дописанная НАД `def main()`, и ни одна работа не трогает `main()`. Конфликты между работами становятся текстовыми (две вставки в одно место), не логическими.

`scripts/mutations.py` — так же append-only: новая цель дописывается в конец списка `ЦЕЛИ` (начало 126). Формат цели: `(имя, файл, кусок-оригинал, кусок-подделка[, ожидаемая строка в выводе])`.

**Р0.2. Часы.** Ни одна работа не пишет в файл монотонное `t` (`face/face.py:2192/2251`). Перевод: `настенное = сейчас_wall - (t - значение)`; чтение обратно; отрицательные и старше суток — выбрасывать. Никто не переводит сценариста на настенные часы внутри кадра.

**Р0.3. Место для файлов состояния — только `~/.robot-ai/`.** `/run` — tmpfs, см. довод в `face/face.py:2143-2166`. Запись — атомарно (`.tmp` + `os.replace`), образец `voice/robot_voice/atlas.py:1070-1101`.

**Р0.4. Зерно.** Умолчания `random.Random(11)` (`scenes.py:1436`), `Random(13)` (`character.py:168`), `Random(7)` (`face.py:428`) НЕ менять — на них стоят проверки (`pc/selftest.py:2244, 2658`, `pc/page_selftest.py:619`). Зерно подставляется только в вызывающих местах.

**Р0.5. Чистые функции остаются чистыми.** `character._рассеян`, `_саккада`, `_фиксация`, `_жребий` (361-425) — без состояния и без Random: робот и ПК считают их независимо при общем `t`. 1/f-шум строить из `_жребий`, не из генератора.

**Р0.6. Зафиксированные интерфейсы между волнами** (пишутся против них, даже если файл ещё не сдан):

```
face/clock.py:   верно() -> bool;  отметить() -> None;  почему() -> str
face/memory.py:  class Память(файл, версия=1)
                   .прочитать() -> dict            # битое/чужое → {}
                   .писать(данные, принудительно=False) -> bool   # ПИСАТЬ_РАЗ_В = 60.0
                 зерно_робота() -> int
                 в_настенное(t, значение, сейчас) -> float | None
                 из_настенного(настенное, t, сейчас) -> float | None
                 остыло(значение, было, сейчас, полураспад) -> float
face/mood.py:    class Настроение
                   .кадр(dt, t, час) ; .добавить(ось, сила, t) ; .ярлык() -> str
                   .было(секунд) -> float ; .снимок()/.восстановить(d)
                   поля: радость уверенность общительность возбуждение доверие
                         долг_сна бодрость
face/learn.py:   class Опыт
                   .множитель(имя, признаки, контекст, t) -> float   # 0.4..2.0
                   .наблюдение(имя, признаки, контекст, награда, t) -> None
                   .распорядок(час, выходной) -> float               # 0..1
                   .сводка(сколько=10) -> list[str] ; .снимок()/.восстановить(d)
face/library.py: СЦЕНКИ, ПО_ИМЕНИ, Сценка, _с + все константы параметров
                 (scenes.py ре-экспортирует их: внешние импорты не меняются)
Сценарист:       .снимок(t, сейчас) -> dict ; .восстановить(d, t, сейчас)
                 .бодрость (property 0..1) ; .отклик(с, t) ; .сводка(n)
Питомец:         .бодрость = 1.0 ; .возбуждение = 0.0 (публичные поля)
                 ._дыхание(t, частота=1.0, сила=1.0)
Губы:            .сила(окно=0.5) -> float   # 0..1, средняя громкость
Кисть:           .кадр(холст, состояние, t, яркость, сцена=None)
face.json новое: "касание", "кто", "близко", "где", "часы_верны",
                 "события_таймеров", "срез", "свет", "шум",
                 "делает": {..., "итог": "идёт"|"вышло"|"отказ"}
Сценарист.кадр() дополнительно возвращает "абсолютные": [id, ...]
```

**Р0.7. Порядок.** Волна 1 — до всего: сохранять и учить на несверенных часах и без сторожей бессмысленно. Волна 4 не начинается, пока волна 1 не в дереве.

---

## ВОЛНА 1 — ФУНДАМЕНТ И ЧЕСТНОСТЬ

Цель волны: перезапуск перестаёт быть перерождением, а тихая поломка перестаёт быть тихой.

### 1A. Память и часы (новые модули)
**ФАЙЛЫ:** `face/memory.py` (новый), `face/clock.py` (новый).

**ДЕЛАТЬ:**
1. `memory.py`: класс `Память` по образцу `voice/robot_voice/atlas.py:1070-1101` — `makedirs` → `.tmp` → `json.dump` → `os.replace`, дросселирование `ПИСАТЬ_РАЗ_В = 60.0`, `принудительно=True` на выходе. Поле `версия`; чужая версия, битый JSON, отрицательные числа — вернуть `{}` молча (лицо обязано стартовать на авторских весах).
2. `зерно_робота()`: читает поле `зерно` из `~/.robot-ai/сценарист.json`; нет — заводит из `/etc/machine-id` + `time.time_ns()`, кладёт в тот же словарь. Стабильно между запусками одного робота, разное у разных.
3. Время: `в_настенное/из_настенного` (Р0.2) и `остыло(значение, было, сейчас, полураспад) = значение * 0.5 ** ((сейчас-было)/полураспад)` — одна функция и для привыкания (900 с), и для статистики обучения (недели).
4. `clock.py`: `верно()` = существует `/run/systemd/timesync/synchronized` ИЛИ `time.time()` больше «последнего виденного времени» из `~/.robot-ai/часы.json` (fake-hwclock: `отметить()` переписывает раз в 5 минут). `почему()` — строка для журнала.

**ВИДНО:** пока ничего (фундамент). **ИИ:** нет.

**ПРОВЕРКИ** (`voice/selftest.py`, новые `test_память_переживает_обрыв`, `test_часы_не_сверены_молчат`):
- запись прервана на `.tmp` — на месте целый старый файл;
- версия схемы чужая → `прочитать()` даёт `{}` и не кидает;
- `в_настенное/из_настенного` туда-обратно на сдвиге часов ±3 ч: значения старше суток отбрасываются;
- `зерно_робота()` дважды подряд — одно число; после удаления файла — другое;
- `верно()` = False без маркера и без файла отметок.

**МУТАЦИИ:** (а) `os.replace(...)` → `shutil.copy(...)` — должна упасть проверка обрыва; (б) в `прочитать()` убрать проверку версии (`if данные.get("версия") != ВЕРСИЯ:` → `if False:`) — падает проверка чужой схемы.

### 1B. Экран робота: сторожа живости, тач, зерно, часы
**ФАЙЛЫ:** `face/face.py`, `systemd/robot-face.service`.

**ДЕЛАТЬ:**
1. **Пиксельный сторож** (главный цикл, рядом с отчётом 2277-2283): раз в секунду `pygame.transform.smoothscale(экран, (32,32))` + хэш байтов. Не менялся дольше `ЗАСТЫЛО = 5.0` при состоянии, которое обязано двигаться (не `кадр_отказа`) — печатать один раз: `лицо: картинка не меняется 5 с (эмоция=…, источник=…, сценка=…)`, дальше повтор раз в минуту.
2. **Счётчик сценок раз в минуту** — перенести механику `pc/kuzya_pc.py:2141-2166` в лицо: печатать рядом с «N к/с» ещё «сценок за 60 с — N, сейчас „имя“, источник — …» (источник уже считается, 2258-2268).
3. **Вынести тик сценариста из `Кисть.кадр`**: сейчас единственный вызов `self.сценарист.кадр(...)` на 1317, а `кадр_из_потока` (1434) и `пусто` (1483) его не зовут — при живом ПК сценарист не тикает вообще. Звать его в `главный()` рядом с 2251 безусловно, результат передавать: `кисть.кадр(экран, состояние, t, доля, сцена=сцена)` (и в `кадр_из_потока`/`пусто` — только для пузыря).
4. **Тач.** `Тач.__init__:1801` — очередь `(x, y, t)` с пределом 32; `_пакет_кончился:1888` кладёт время; `нажатия():1902` выбрасывает всё старше 1.0 с и печатает «N нажатий просрочено». После возврата из видео (2219) чистить очередь и `кисть.кнопки`.
5. **Кнопки только там, где их видно.** `_виджеты:1263-1293` — при `яркость ≤ 0.02` не наполнять `self.кнопки` (отрисовку НЕ трогать, иначе `_переход:577` проиграет уезд в темноте). В `Яркость` (1509) — метод `разбудить()`; в главном цикле (2237) касание при нулевой яркости будит экран и съедается, строка «лицо: нажали на погасший экран — бужу».
6. **Зерно.** `Кисть.__init__:427` — `случай=None`, в теле `self.случай = случай or random.Random(memory.зерно_робота())`; `главный():2177` ничего не передаёт. При старте печатать `лицо: жребий 0x…`.
7. **Часы.** `главный():2235` — если `clock.верно()` False, класть `состояние["время"] = ""`, `состояние["часы_верны"] = False` и передавать в сценарий `час=-1`; при смене состояния часов — одна строка в журнал. Звать `clock.отметить()` раз в 5 минут.
8. **Шрифт.** `__init__:432-433` — печатать, какой файл взят и есть ли жирный; в `_шрифт:505` один раз отрисовать пробу «Ру0» и сравнить растр с U+FFFF — совпало, значит кириллицы нет → `log.error` и явный откат.
9. Удалить дубль `_зет` (`face.py:945-958` — точная копия 930-943).
10. `systemd/robot-face.service`: в `[Unit]` — `StartLimitIntervalSec=0` с комментарием, как в `robot-voice.service:4-6`.

**ВИДНО:** экран будится пальцем; музыка больше не выключается тычком в темноту; после мультика робот не исполняет пачку старых нажатий; в журнале видно, что персонаж жив (сценок/мин) и что он замер, если замер.

**ИИ:** нет.

**ПРОВЕРКИ** (`voice/selftest.py`, внутрь/рядом с `test_лицо_робота:10357`):
- `нажато()` возвращает `""` при яркости 0; после `разбудить()` — действие возвращается;
- нажатие возрастом 1.5 с не исполняется, возрастом 0.2 с — исполняется;
- два кадра подряд с одинаковым состоянием при эмоции «спокоен» дают РАЗНЫЕ 32×32-хэши (то есть сторож не будет ложно срабатывать), а замороженный кадр — одинаковые, и сторож даёт строку;
- ast-проверка: ни в одном модуле лица нет двух определений одного метода в классе (тот же скрипт, которым найден `_зет`);
- `Кисть(шрифт="/нет/такого")` печатает строку про откат;
- `robot-face.service` содержит `StartLimitIntervalSec=0`.

**МУТАЦИИ:** (а) сторож застывания `if прошло >= ЗАСТЫЛО:` → `if False:`; (б) `_виджеты`: убрать ранний выход по яркости; (в) в `нажатия()` убрать отсечку по сроку.

### 1C. Сторож молчания детектора тел
**ФАЙЛЫ:** `voice/robot_voice/ros.py`, `scripts/checkup.py`.

**ДЕЛАТЬ:**
1. `ros.py:_on_open:332` — запоминать время подписки на `ЛЮДИ` (362) и завести счётчик принятых сообщений по топику (инкремент в 398).
2. Отдельный сторож (поток или проверка в существующем цикле раз в 30 с): ноль сообщений через 30 с после подписки → `log.error("детектор тела: подписка оформлена 30 с назад, не пришло ни одного сообщения (проверь QoS best_effort и journalctl -u robot-body)")`, дальше повтор раз в 5 минут. Сторож живёт у ПОДПИСКИ, не в `vision_track._считать_диагностику:689` — та по построению зовётся только на пришедшем сообщении.
3. `checkup.py:проверить_зрение:444` — печатать «топик молчит N с по мнению самого робота».

**ВИДНО:** пропажа 151 сценки из 295 перестаёт быть немой — в журнале строка с готовым рецептом.
**ИИ:** нет.

**ПРОВЕРКИ** (`voice/selftest.py`, `test_молчащая_подписка_видна`): подделка моста, подписка оформлена, сообщений нет → ровно одна строка error за 30 с и ещё одна через 5 минут; при идущих сообщениях — ни одной.
**МУТАЦИИ:** условие «ноль сообщений после подписки» → `if False:`.

### 1D. Издатель состояния: ничего не терять и не выдумывать
**ФАЙЛЫ:** `voice/robot_voice/face.py`.

**ДЕЛАТЬ:**
1. **Очередь вместо последнего значения.** `делает():318` и `праздник():327` кладут в `deque(maxlen=8)`; `состояние():653` снимает по одному элементу за такт (`ПИШЕМ_РАЗ_В = 0.1`, 115). Образец удержания — `motion.py:99-105`. Счётчик выброшенных по переполнению, раз в минуту строка «лицо: N жестов не доехало до экрана».
2. **Эмоция живёт минимум один такт**: новая эмоция не заменяется раньше следующей публикации (иначе «думаю» из кэша быстрее 100 мс не попадает ни в один снимок и четыре сценки «думает» не играют никогда).
3. **Края таймеров считает тот, кто знает правду.** `_таймеры():621` — вместо угадывания по срезу `[:3]` отдавать явное поле `"события_таймеров": ["поставлен"|"кончился"|"скоро"]` (издатель помнит прошлый снимок) и `"срез": True`, когда список усечён; поле `"пауза"` оставить.
4. **Итог дела:** в словарь `делает` добавить `"итог"`: `"идёт"` при вызове, `"вышло"`/`"отказ"` — когда инструмент вернулся.
5. **Новые поля состояния** (заполняются настоящими данными, не заглушками): `"кто": {"имя","встреч","свой","давно","видел_сегодня"}` из `people.py`; `"близко": метры` из размера рамки детектора; `"где": "кухня"` из `landmarks.py`; `"часы_верны"` из `face/clock.py`.

**ВИДНО:** навык из восьми шагов даёт восемь жестов, а не один; «дзинь» звучит на настоящий конец таймера, а не на выпавший из среза; персонаж узнаёт, кто перед ним.
**ИИ:** нет (узнавание голоса локальное).

**ПРОВЕРКИ** (`voice/selftest.py`, рядом с издателем в `test_лицо_робота`):
- два `делает()` внутри одного такта → два разных снимка подряд, оба имени сохранены, ни одно не затёрто;
- четыре идущих таймера, ближайший отзвонил → `события_таймеров == ["кончился"]`, `срез == True`, «поставлен» не появляется;
- таймер на паузе с 8 с не даёт «скоро»;
- `кто` пустое, когда `people.py` не знает человека, и не роняет состояние.

**МУТАЦИИ:** (а) вернуть `self._делает = ...` вместо очереди; (б) вернуть `(идут + стоят)[:3]` без пометки `срез`.

### 1E. ПК: сценарист тикает сам, застывшая страница видна
**ФАЙЛЫ:** `pc/kuzya_pc.py`, `pc/avatar/index.html`.

**ДЕЛАТЬ:**
1. `Аватар` (1837): вынести тик в поток 20 Гц (`_тик()`), `состояние():1988` сделать чистым чтением последнего тика. Сейчас сценарист двигается только когда кто-то дёрнет `/avatar/state` (страница раз в 100 мс, `index.html:670`) или `/tts` (2766 — ради одного поля эмоции): закрыли вкладку — персонаж замер на полужесте.
2. `Съёмка.положить` (2115-2120): считать `hash(jpeg)`; одинаковый дольше `ЗАСТЫЛА = 5.0` → `self.беда = "страница застыла"`, одна строка в лог и ПЕРЕСТАТЬ обновлять `self.когда` (кадр начинает честно стареть, робот через `ПОТОК_СТАРЕЕТ = 1.5` уходит на своё лицо). Сторож стоит на ПК, где известно, что страница обязана двигаться, — не на роботе (там повтор кадра штатный, `ТИШИНА = 1.0`, 2091).
3. `index.html`: `window.__пульс = {кадров, состояний}` рядом с `кадров_из_события` (367); `_жалоба_страницы` (2216-2221) при осмотре раз в 5 с (2296) сравнивает — не выросло, значит «страница не рисует».
4. `Сценарист` на ПК создавать с зерном: 1875 и 3235 → `scenes.Сценарист(random.Random(memory.зерно_робота()), праздники=...)`; `Питомец` 1862 — тоже `случай=`.

**ВИДНО:** Марк не замирает при закрытой вкладке; робот не показывает вечно один и тот же «живой» кадр с умершей страницы.
**ИИ:** нет.

**ПРОВЕРКИ** (`pc/selftest.py`, рядом с `test_avatar_stream:1191`): подкладываем один и тот же JPEG 6 секунд → `свежий == False`, в логе одна строка; тик идёт без единого вызова `состояние()`; два `состояние()` подряд не двигают сценариста дважды.
**МУТАЦИИ:** убрать сравнение хэшей в `положить`.

### 1F. Сценарист: не врать при пропуске, не молчать, не терять
**ФАЙЛЫ:** `face/scenes.py`.

**ДЕЛАТЬ:**
1. **`_заметить_пропуск(t)` первой строкой `кадр()` (1877)**: помнить `_последний_кадр`; если `t - _последний_кадр > ПРОПУСК = 2.0` — сдвинуть на величину пропуска ВСЕ привязки ко времени (`_играли`, `_следующая`, `_покой_с`, `_человек_с`, `_звал_до`, `_зовёт_с`, `_батарея_жаловался`, `_начало`), обнулить `self._было` (никаких выдуманных событий из диффа со вчерашним состоянием) и напечатать «сценарист: пропуск N с — перематываю, событий за это время нет». Сторож внутри, а не у вызывающего: ровно на «вызывающий забыл» и построена нынешняя дыра.
2. **Никогда не пусто.** `_выбрать():1808` при пустом `годные` сейчас возвращает None. Каскад как у AnimationGroup: (а) отфильтровать по поводу; (б) если все на отдыхе — взять ту, которой меньше всего осталось до выхода с отдыха; (в) None только если повода нет в библиотеке вовсе.
3. **Счётчик потерь.** Словарь `_потеряно[событие] += 1` в двух местах: событие не влезло в кадр (цикл 1909-1930, `break`) и `_выбрать` вернул None. Раз в минуту строка «сценарист: не отыграно — таймер_кончился 2, делает_музыку 1».
4. **Расписание — помечать по факту начала.** `_расписание:1730-1735` сейчас ставит `_было_сегодня` ДО показа: в праздничное утро пятницы три поздравления сгорают разом. Убрать пометку оттуда, ставить в `кадр()` там, где `начато = соб` (1924).
5. **Час «не знаю».** `кадр(..., час=-1)` → `_расписание` возвращает `[]` сразу, `час_пробил` не срабатывает, ночной фон не включается по часу.
6. **Снимок/восстановление.** Методы `снимок(t, сейчас)` и `восстановить(d, t, сейчас)` сразу после `__init__` (после 1479). Сохранять: `_играли`, `_повторов`, `_было_сегодня`, `_текст_праздника`, `_батарея_жаловался`, `_час_был`, `_звал_до`, `_таймер_скоро_был` (списком), `зерно`. НЕ сохранять: `_было`, `_сцена`, `_начало`, `_следующая`, `_пеленг`, `_отложено`, `_план`, `_покой_с`, `_человек_с`, `_зовёт*` — это середина кадра, а не биография. Время — через `memory.в_настенное/из_настенного`; `_повторов` при загрузке домножить на `memory.остыло(..., полураспад=ПРИВЫКАНИЕ_ЗАБЫВАЕТСЯ)`.
7. Выбросить мёртвые поля `_человек_ушёл_в` (1453) и `_день_утра` (1461) — присваиваются и нигде не читаются.
8. `ПРИВЫКАНИЕ_ЗАБЫВАЕТСЯ` (205) поднять с 900 с до 6 ч: для жителя квартиры привыкание к сценке тает сутками, а не за 15 минут (раньше это было бессмысленно — состояние всё равно умирало с процессом).

**ВИДНО:** «С добрым утром!» не повторяется после перезапуска службы; в праздничную пятницу звучат все три поздравления; после часа под потоком с ПК персонаж не выдаёт пачку событий; повод без свободной сценки всё равно чем-то отыгрывается.
**ИИ:** нет.

**ПРОВЕРКИ** (`pc/selftest.py`, внутрь `test_scenes:1515`):
- `кадр()` в t=10, потом в t=3610 при изменившемся состоянии → ноль событий, все отдыхи сохранены относительно, строка про пропуск;
- все сценки повода на отдыхе → `_выбрать` возвращает не None и берёт ближайшую к выходу;
- три годных события расписания в одно утро → три разных сыграны за три кадра, на следующий день ни одно не повторяется;
- `снимок()` → `восстановить()` через 3 часа настенного: «доброе утро» не повторяется, повторы просели по экспоненте, отрицательных отдыхов нет;
- битый словарь в `восстановить()` не кидает и оставляет авторские веса;
- `час=-1` → `_расписание() == []`.

**МУТАЦИИ:** (а) `_заметить_пропуск` → `if False:`; (б) каскад в `_выбрать` → `return None`; (в) вернуть пометку расписания до показа.

### 1G. Измеритель жизни (новый инструмент)
**ФАЙЛЫ:** `scripts/прогон_жизни.py` (новый).

**ДЕЛАТЬ:** headless-прогон сценариста+питомца без pygame: сутки модельного времени за минуту, по сценарию присутствия (человек приходит/уходит, говорит, ночь). Печатает числа раздела 5: сценок в час по часам суток, число разных имён, максимальную долю одного имени, повторы подряд, долю кадров с неподвижной позой (по 32×32-хэшу позы), спектральный пик подскока, задержку отклика на касание, покрытие поводов (сколько поводов имеют <4 сценки). Флаги `--без-пк`, `--сутки N`, `--зерно`.

**ВИДНО:** ничего; это линейка, по которой принимаются остальные волны. **ИИ:** нет.
**ПРОВЕРКИ:** сам себе проверка + `test_измеритель_не_врёт` в `pc/selftest.py`: на заведомо мёртвом сценаристе (заглушка, всегда одна сценка) метрики показывают провал.
**МУТАЦИИ:** не нужны (инструмент, не защита).

---

## ВОЛНА 2 — ТЕЛО, КАСАНИЯ, ВОСПРИЯТИЕ

### 2A. Тело: дышит всегда, движется не по формуле
**ФАЙЛЫ:** `face/character.py`.

**ДЕЛАТЬ:**
1. **`_дыхание` (427-432) → `_дыхание(self, t, частота=1.0, сила=1.0)`** и не одна синусоида, а сумма по образцу Live2D `setupBreath`: подскок (период 3.2345 с), наклон корпуса (15.5345 с), микроповорот головы (6.5345 с), плюс несимметричная кривая вдох/выдох 40/60 (быстрый подъём, медленный спуск, короткая пауза) вместо `sin`. Новые константы рядом с 82-83.
2. **Спящий и встревоженный дышат.** `_поза`: строка 248 → `подскок=self._дыхание(t, СОН_ЧАСТОТА=0.5, СОН_СИЛА=2.5)`; строка 254 → `подскок=self._дыхание(t, ТРЕВОГА_ЧАСТОТА=3.0, ТРЕВОГА_СИЛА=0.6)` плюс тремор ±0.3° на наклон. НЕ через `ЭМОЦИЯ_В_ТЕЛЕ` (109) — у «сплю»/«тревога» ключей там нет намеренно.
3. **1/f вместо суммы синусов.** `_рассеян:361` — `дрейф()` (376) заменить на розовый шум из 5 октав поверх `_жребий`: `sum(_жребий(floor(t*2**k), соль_k)/2**k for k in range(5))`, нормировать в −1..1. Функция остаётся чистой (Р0.5). Константы `РАССЕЯН_ЧАСТОТЫ_*` (88-92) уходят.
4. **Саккады по-человечески.** `СААКАДА_ШАГ` (100) 0.85 → 0.22 с, `СААКАДА_ДЕРЖИТ_ПОДРЯД` (103) 3 → 8: фиксации становятся непрерывно распределены в 200-600 мс вместо квантов 0.85/1.7/2.55. `_саккада:418` — длительность перелёта функцией амплитуды (30 мс на малый скачок, 120 мс на край поля) вместо постоянных 0.08. Между перелётами добавить микродрожь ±0.015 из того же 1/f — сейчас взгляд между прыжками стоит абсолютно мёртво.
5. **Бюджет контакта глаз.** `_смотрит:353` — таймер контакта по слотам `_жребий`: 3-5 с в упор, потом отвод 0.8-2.0 с, потом возврат; доля отвода не константа `ВЗГЛЯД_ОТВОД = 0.25`, а функция состояния: «думаю» 0.8-1.0 и вверх, «огорчён»/«не_понял» — вниз, речь — вбок в паузах.
6. **Походка.** `_шагнуть:486-503`: подскок считать от фазы ноги (`abs(sin(self._нога_фаза * pi))`), а не от абсолютного времени — сейчас около шести подскоков на переступ; `ПОСТУПЬ_ЧАСТОТА` (79) уходит. Добавить противофазный мах рук от той же фазы, амплитуда 12-15° (сейчас у идущего рук на экране нет вовсе: `_пустая_поза` кладёт нули, а `face.py:_руки:883` не рисует угол <0.5°).
7. **Бодрость и возбуждение как входы.** Публичные поля `self.бодрость = 1.0`, `self.возбуждение = 0.0` в `__init__` (170). Потребители: `_дыхание` (частота ×(2−бодрость)), `СКОРОСТЬ_ХОДЬБЫ` в `_шагнуть:489` (× бодрость, но не ниже 0.6 — ниже начнётся спор с `МИН_СКОРОСТЬ_ДОЛЯ = 0.18` и «замер в полупикселе»), границы жребия `В_ИДЁТ/В_СТОИТ` в `_решить:450` (усталый чаще стоит), `СААКАДА_ШАГ` (реже переводит взгляд).
8. **Изменяемое умолчание.** `__init__:168` `случай=random.Random(13)` вычисляется один раз при импорте, и все Питомцы без зерна делят генератор. `случай=None` в сигнатуре, `self.случай = случай or random.Random(13)` в теле (170).

**ВИДНО:** спящий поднимается и опускается; встревоженный дышит часто и мелко с дрожью; взгляд не ездит маятником и не стоит мёртво; идущий машет руками, и подпрыгивает ровно на шаге; к вечеру всё медленнее.
**ИИ:** нет.

**ПРОВЕРКИ** (`voice/selftest.py`, `test_тело_дышит_всегда`, `test_взгляд_по_человечески`):
- ни одна поза при любой комбинации полей не даёт постоянный подскок на окне 10 с (max−min > 0.001·H) — включая «сплю» и «тревога»;
- автокорреляция подскока за 10 минут не имеет пика > 0.5 на лагах 1-60 с;
- распределение длин фиксаций: медиана 200-600 мс, доля длин >1.5 с не более 15%;
- контакт глаз: за 60 с непрерывного присутствия человека число отводов ≥ 12, самый долгий взгляд в упор ≤ 6 с;
- при `бодрость = 0.4` скорость хода падает не более чем вдвое и персонаж доходит до цели за конечное число кадров (защита от «замер в полупикселе»);
- два Питомца без зерна дают независимые последовательности.

**МУТАЦИИ:** (а) вернуть `подскок=0.0` в ветку «спит»; (б) вернуть `sin(t*2*pi*ПОСТУПЬ_ЧАСТОТА)` в `_шагнуть`; (в) сделать 1/f одной октавой (`range(5)` → `range(1)`).

### 2B. Экран робота: касание доходит до персонажа, голос трясёт тело
**ФАЙЛЫ:** `face/face.py`, `face/lips.py`.

**ДЕЛАТЬ:**
1. **Касание в состояние.** Главный цикл 2237-2250: `действие = кисть.нажато(tx, ty)` пусто — не `continue`, а положить в состояние перед отрисовкой: `состояние["касание"] = {"номер": n, "куда": "голова"|"тело"|"рука"|"пол"|"мимо", "где": (tx-поза_x)/H, "вид": "тычок"|"поглаживание"|"держат", "секунды": s}`. Зона считается от `self.поза["x"]` (выставлена наружу, 500-502) и `ТЕЛО_ОТ_ВЕРХА/ТЕЛО_ВЫСОТА`. Вид различает уже готовый `Тач` (1781-1958: `ТАП_НЕ_ДОЛЬШЕ = 0.6`, `ТАП_НЕ_ДАЛЬШЕ`), нужна только скорость свайпа: 1-10 см/с — поглаживание, быстрее/короче — тычок.
2. `причесать:341-369` — «касание» в список словарных ключей (353), иначе кривое поле дойдёт до сценариста.
3. **Реакция в том же кадре.** Отклик на касание рождается здесь и в `character.py`, с жребием задержки 0.2-0.8 с на «осознание», и НИКОГДА не ждёт режиссёра.
4. **Бодрость по проводу:** перед 1312 — `self.питомец.бодрость = self.сценарист.бодрость` и `self.питомец.возбуждение = self.сценарист.возбуждение`.
5. **`lips.py`: метод `сила(окно=0.5)`** рядом с `рот()` (77-87) — средняя громкость последних полусекунды. В `face.py:1337-1339` домножать на неё амплитуду дыхания и подскока: на громком слове персонаж чуть подаётся вперёд и вздрагивает всем телом, а не двигает одной челюстью.
6. **Свои праздники на роботе.** `face.py:456` — читать «праздники» из `~/.robot-ai/сценарист.json` и передавать вторым параметром в `scenes.Сценарист(...)`. Сейчас день рождения хозяина знает только Марк на ПК (`pc/kuzya_pc.py:1718-1723, 3235`) — то есть ровно то тело, которое без ПК не работает.

**ВИДНО:** гладишь по голове — жмурится и льнёт; тычок в бок — вздрагивает и через полсекунды ищет глазами человека (не место касания); на громкой фразе вздрагивает всем телом; день рождения празднуется и без ПК.
**ИИ:** нет.

**ПРОВЕРКИ** (`voice/selftest.py`): касание в координату головы даёт `куда == "голова"` при персонаже слева и справа экрана; медленный свайп → «поглаживание», быстрый тап → «тычок»; `сила()` на тихой и громкой фразе отличается ≥ вдвое; `Сценарист` на роботе получает праздники из файла.
**МУТАЦИИ:** (а) вернуть `continue` на пустое действие; (б) убрать передачу праздников.

### 2C. Сценарист: восприятие (касания, люди, слух, честность дел) + разрез библиотеки
**ФАЙЛЫ:** `face/scenes.py`, `face/library.py` (новый).

**ДЕЛАТЬ:**
1. **Разрез (делать первым).** Перенести в `face/library.py`: константы параметров (67-92), `класс Сценка` (223-274), `_с` (277), `СЦЕНКИ` (284-1425), `ПО_ИМЕНИ`, `ИЩЕТ`, `ПРАЗДНИКИ`, `ДЕЛА`/`дело()`. `scenes.py` импортирует и ре-экспортирует их: `from library import *` плюс явный список — ни один внешний импорт (`face/face.py`, `pc/kuzya_pc.py`, оба selftest'а) не меняется. После этого данные и логика правятся параллельно — это условие волн 3 и 4.
2. **Касания как события.** В `СОБЫТИЯ` (94-122) добавить «погладили_голову», «погладили_тело», «ткнули», «щекочут», «держат». В `_события` — блок рядом с 1619 (сравнение номеров, как у «делает»). Лестница ласки по образцу Vector: `_ласка: dict[зона, float]` с экспоненциальным забыванием (та же `memory.остыло`), 2 поглаживания поднимают уровень; пять уровней — цепочкой через `следом`. Плюс быстрое «терпение» (падает от затыкивания, восстанавливается за минуты) и медленная «привязанность» (только копится, в снимок).
3. **Кто перед нами.** `_события:1575-1585` — вместо одного «человек_пришёл» выбирать «хозяин_пришёл» / «гость_пришёл» / «давно_не_виделись» (порог из `с["кто"]["давно"]`). Порог разлуки растёт с возрастом робота: <24 ч жизни — 30 мин, <168 ч — 1 ч, <336 ч — 2 ч, дальше 4 ч. Подстановка `{имя}` в тексте сценки — тем же приёмом, что `_текст_праздника` (кадр(), 1992-1994).
4. **Слух.** `_события:1685-1695` — убрать `break` (сейчас «Спасибо, пока!» отыгрывается только как «спасибо»), расширить таблицу до 25-30 подстрок (взять выверенные формулировки из `voice/robot_voice/intents.py:1048-1106`): да, нет, стоп, прости, доброе утро, спокойной ночи, как дела, своё имя, ругань. Все новые поводы дописать в `ОТКЛАДЫВАЕМЫЕ` (123) — иначе второе и третье совпадение погибнут под сценкой «имя» (причина расписана в комментарии 108-122).
5. **Честность дел.** Читать `делает["итог"]` (от 1D): «вышло» → короткий кивок, «отказ» → развёл руками; отказ можно связать цепочкой с уже написанными сценками «огорчён» (788, 791, 1171, 1174). «Не расслышал» должно давать «не_понял», а не спокойное лицо.
6. **Края таймеров** читать из `с["события_таймеров"]`, а при `с["срез"]` не выдавать «таймер_поставлен»/«таймер_кончился» и один раз написать в журнал.
7. **Расстояние.** `"близко"` — пороговые события «подошёл вплотную» / «отошёл далеко» по образцу `_события:1595-1607` (накопление и сравнение с порогом, как у пеленга).
8. **Ночь и скука.** `_фоны:1764-1791`: при человеке ночью возвращать `["человек_рядом_ночью", "человек_рядом"]`; добавить «покой_вечером» (17-23) и «покой_днём»; скуку сделать ступенчатой — при `t - _покой_с >= ДОЛГО_ОДИН` → `["долго_один", "покой"]` (сейчас она приписана в хвост и получает втрое меньший вес), при `>= ОЧЕНЬ_ДОЛГО_ОДИН = 1800` → `["долго_один"]`. Границы часов вынести в константы рядом с 216, чтобы `_фоны`, `_расписание` (1699) и `director.время_суток` (47-54) брали одни числа.

**ВИДНО:** робота можно потрогать, и он отвечает по-разному в зависимости от места и настойчивости; «Привет, Игорь!» вместо «О, привет!»; «Спасибо, пока!» даёт и поклон, и взмах; отказ инструмента выглядит отказом; в 2:30 ночи подошедший получает ночной набор, а не дневную возню; через полчаса одиночества персонаж томится, а не потягивается.
**ИИ:** нет.

**ПРОВЕРКИ** (`pc/selftest.py`, `test_scenes`):
- «спасибо, пока» даёт два события, второе приходит следом в пределах `ОТКЛАДЫВАТЬ_НЕ_ДОЛЬШЕ`;
- пять поглаживаний подряд поднимают уровень до 3+ и дают разные сценки; двадцать подряд не дают раздражения дольше 60 с (терпение восстанавливается);
- «свой» и «чужой» на одном и том же событии дают разные пулы;
- при `срез=True` таймерные события не выдаются;
- в 2:30 из выбранных за 10 минут сценок ни одна не из дневного пула;
- после 1800 с одиночества доля сценок «долго_один» ≥ 0.9;
- ре-экспорт: `scenes.СЦЕНКИ is library.СЦЕНКИ`, `len(scenes.СЦЕНКИ)` не изменился, все внешние импорты живы.

**МУТАЦИИ:** (а) вернуть `break` в разбор услышанного; (б) вернуть `фоны.append("долго_один")` в хвост; (в) игнорировать `срез`.

### 2D. Страница Live2D: слои, дыхание, моргание, неровные жесты
**ФАЙЛЫ:** `pc/avatar/index.html`.

**ДЕЛАТЬ:**
1. **Слои с фейдом вместо Overwrite.** `кадр_без_защиты` (472+): сейчас каждый кадр `цель[id] = 0` для всего (495-496), потом поверх эмоция/сценка — это подмена без фейда. Ввести три слоя с весами: фон-дыхание → эмоция → сценка, передача управления параметром — фейдом 0.2-0.3 с (правило VTube Studio: никогда не мгновенная установка).
2. **Дыхание пятью параметрами** теми же числами, что в `character.py` (2A): ParamAngleX 0/15/6.5345, ParamAngleY 0/8/3.5345, ParamAngleZ 0/10/5.5345, ParamBodyAngleX 0/4/15.5345, ParamBreath 0.5/0.5/3.2345 — чтобы оба тела дышали одинаково.
3. **Своё моргание** через ParamEyeLOpen/ParamEyeROpen с фазами 0.1/0.05/0.15 с и интервалом `t + r*(2*interval − 1)`; авто-моргание модели отключить, чтобы не спорили два источника. Интервал — функция эмоции: спокоен 4±1 с, встревожен 2±0.5 с, «думаю» чаще, при `с.говорит` — подавлять. Веки левое и правое с разбегом 20-30 мс.
4. **Жесты покоя неровно.** `срок_жеста()` (421) берёт равномерный интервал [10,25] — заменить на экспоненциальный с тем же средним (как `scenes._пауза`, 1793).
5. **«Думаю» — движение, а не статика.** `ЭМОЦИИ["думаю"]` (307+) сейчас статично уводит ParamEyeBallX/Y: сделать отвод вверх с возвратом, 0.9-1.5 с.

**ВИДНО:** Марк перестаёт дёргаться на стыках сценок; дышит всем телом вразнобой; моргает по настроению; жесты покоя не идут по расписанию.
**ИИ:** нет.
**ПРОВЕРКИ** (`pc/page_selftest.py`, настоящий Chromium): за 60 с зафиксировать ParamAngleX/Y/Z и ParamBreath — ни один не постоянен, автокорреляция без пика; переход эмоции не даёт скачка параметра больше X за кадр; интервалы жестов покоя за 30 минут имеют разброс ≥ 0.8 от среднего.
**МУТАЦИИ:** вернуть равномерный `срок_жеста` — падает проверка разброса.

### 2E. Режиссёр без модели
**ФАЙЛЫ:** `face/director.py`, `pc/kuzya_pc.py`.

**ДЕЛАТЬ:**
1. `director.py` — новая функция рядом с `запрос` (96-116): `план_по_правилам(с, сценарист, t, час) -> (имена, реплика)`. Берёт `сценарист.годные(с, t, час)` (уже отсортирован по важности фона и давности, см. докстроку 1505-1519) и собирает 2-4 имени по правилам: молчит рядом дольше `ВНИМАНИЕ_ЧЕРЕЗ` — два зова плюс спокойная; никого дольше `ДОЛГО_ОДИН` — скучающая плюс потягивание; на экране карточка — жест к ней плюс реакция; ночь — не звать. Безопасно: `задать_план` (1482-1495) всё равно фильтрует по `ПО_ИМЕНИ`.
2. `pc/kuzya_pc.py:_позвать_режиссёра` (1904-1934): модель молчит, не ответила за срок или `спросить_режиссёра is None` → `план_по_правилам`. Логировать, каким путём получен план.
3. На роботе: `face/face.py` директора не импортирует вовсе — подключение отложить до волны 4 (там же владелец face.py), а пока обеспечить, чтобы `план_по_правилам` не требовал ничего, кроме `scenes`.

**ВИДНО:** без ПК и без интернета персонаж собирает связки по ситуации, а не валится в чистый жребий.
**ИИ:** нет (это и есть замена ИИ).
**ПРОВЕРКИ** (`pc/selftest.py`, `test_director:2528`): при молчащей модели план не пуст и состоит только из существующих имён; при человеке молча рядом ≥1 зова в плане; ночью зовов в плане нет; план по правилам и план модели неотличимы по типу для сценариста.
**МУТАЦИИ:** сделать `план_по_правилам` возвращающим `[]` — падает проверка «без модели план есть».

### 2F. Настроение и обучение как чистая математика (новые модули)
**ФАЙЛЫ:** `face/mood.py` (новый), `face/learn.py` (новый).

**ДЕЛАТЬ:**
1. `mood.py`, класс `Настроение`: пять осей −1..+1 (радость, уверенность, общительность, возбуждение, доверие; у возбуждения и доверия диапазон 0..1). Два закона затухания: TimeRatio (узлы (0,1)(10,1)(30,0.9)(75,0.6)(150,0)) и ValueSlope (возбуждение: при 0 — 0.6/мин, на 0.2-0.8 — 0.12/мин, выше 0.8 — 0.4/мин; доверие — 0.0, не тает никогда). `добавить()` сбрасывает часы затухания только при `|delta| > 0.05` и только если толчок гонит прочь от нуля. `ярлык()` → «тихо»/«средне»/«живо»/«расстроен», причём «тихо» проверяется ПЕРВЫМ (возбуждение ≤ 0.2) — встроенный предохранитель от навязчивости. Кольцевой буфер на 256 отсчётов и `было(секунд)` — для реакций на производную («вдруг обрадовался», «сник»).
2. Долг сна: бодрствование копит впятеро быстрее, чем сон гасит; потолок 20 ч; гистерезис — засыпать при долге > 1 ч, просыпаться при < 30 мин; при засыпании поднимать долг минимум до 50 мин; 10 минут запрета на повторное засыпание. `бодрость` = производная величина 0..1 плюс суточная кривая по часу (подъём к 9-11, провал 14-16, спад после 22).
3. `learn.py`, класс `Опыт`: Beta-Бернулли на сценку (α, β, когда_обновлено), априор из авторского веса (`p0 = clip(вес/2, 0.2, 0.8)`, сила 8-12 псевдонаблюдений), выборка `betavariate`, множитель `clip(1 + 1.0*(θ − p0), 0.4, 2.0)`, ε-пол 0.1 (каждый десятый выбор идёт мимо обучения). Групповой уровень (пул `когда` + автопризнаки) с усадкой листа к родителю. Базовые линии контекста (фон × блок суток) и обучение на преимуществе над линией. Затухание с полураспадом 14-30 суток — числитель и знаменатель. Распорядок: 24 часа × {будни, выходные} = 48 клеток, `P ← f·P + (1−f)·S`, f≈0.9, add-1 сглаживание, усадка к средней по часу; предсказания молчат первые двое суток. Всё — на достаточных статистиках, без журнала событий.

**ВИДНО:** пока ничего (подключение в волнах 3-4). **ИИ:** нет.

**ПРОВЕРКИ** (`pc/selftest.py`, `test_настроение`, `test_опыт`):
- шум ±0.03 сто раз подряд не продлевает возбуждение — оно остывает по графику;
- возбуждение из середины (0.5) держится ≥ 3 минут, с пика 0.95 спадает за минуту, с 0.05 — за секунды;
- доверие не тает за сутки; растёт по +0.1 и падает в ноль от одного рывка;
- долг сна: сутки бодрствования → сонливость; 20 минут сна не будят обратно (гистерезис);
- бандит: 50 наблюдений «хорошо» поднимают множитель, но не выше 2.0; 50 «плохо» не опускают ниже 0.4; ε-пол даёт ≥8% выборов по авторским весам на 1000 ходов;
- три наблюдения не сдвигают множитель больше чем на 15% (сила априора);
- забывание: 30 суток без наблюдений возвращают множитель к 1.0 ± 0.05.

**МУТАЦИИ:** (а) убрать порог 0.05 в `добавить`; (б) снять клип множителя; (в) убрать затухание статистики.

---

## ВОЛНА 3 — ХАРАКТЕР И РАЗНООБРАЗИЕ

### 3A. Сценарист: приоритеты вместо весов, группы вместо одиночек, настроение вместо строки
**ФАЙЛЫ:** `face/scenes.py`.

**ДЕЛАТЬ:**
1. **Подключить `mood.Настроение`**: создать в `__init__`, обновлять первой строкой `кадр()` после `_заметить_пропуск`. Событие → `добавить(ось, сила)` по таблице (ласка: доверие +0.1, радость +0.1, общительность +0.2, возбуждение −0.08; имя +возбуждение; отказ инструмента — уверенность −; успех +). `ярлык()` входит в `_фоны` как дополнительное измерение; `бодрость` — публичное свойство `Сценарист.бодрость` (его читает `face.py`).
2. **Приоритет фона — фильтром, а не весом.** Убрать `ГЛАВНЫЙ_ФОН_ТЯЖЕЛЕЕ = 3.0` (204) из `_выбрать:1814-1828`: запасной фон берётся только если у главного нет отдохнувших сценок (кулдаун — жёсткий фильтр, вес — мягкое предпочтение).
3. **Короткая лестница приоритетов перед жребием** в `кадр()`: список правил «условие → группа», у каждого правила свой кулдаун с разбросом ±50%; жребий остаётся последним пунктом лестницы. Разнообразие рождается из истощения приоритетов, а не из костей.
4. **Группы вариантов вместо «повод → одна сценка».** Поле `Сценка.группа`; повод называет группу; режимы группы: «колода» по умолчанию (обойти все, прежде чем повторить), `sequential` для реакций на касание (первый тычок — удивился, третий — сердится), `norepeat` для праздников.
5. **Отдых — словарь по фону:** `Сценка.отдых` может быть `{"покой": 90, "человек_рядом": 300}`; читать в `_выбрать` и `годные` (1501) по текущему главному фону.
6. **Привыкание по узлам:** необязательное `Сценка.привыкание` — список узлов (секунды → доля эффекта); у сценок на «имя», «услышал_привет», «погладили» ставить единицу всегда (к собственному имени не привыкают). Глобальные `ПРИВЫКАНИЕ_СИЛА`/`ЗАБЫВАЕТСЯ` остаются умолчанием.
7. **Неидеальная отзывчивость:** `Сценка.вероятность` 0.7-0.9 для мелких поводов, 1.0 для важных (тревога, батарея, имя) — жребий в `кадр()`. Робот, реагирующий на 100% мгновенно, читается как кнопка.
8. **Три класса перебоя:** поле `прерывает` = «сразу» / «по-доброму» (ждать ближайшей мягкой точки внутри сценки) / «дождаться». Сейчас событие с приоритетом 2 режет любую фоновую немедленно, и длинные выразительные сценки почти никогда не досматриваются.
9. **Мягкие пожелания:** `Сценка.предлагает` = (имя фона, срок годности) → `self._пожелание`, читается в `_фоны` до всего остального и протухает. Это же канал для режиссёра — пожелание, а не приказ.
10. **`"абсолютные"` в выдаче `кадр()`** — список, который страница читает вместо своего хардкода (снимает вечную грабли «АБСОЛЮТНЫЕ знают два файла»).
11. Долг сна из `mood` заменяет `НОЧЬ_СПИТ_ЧЕРЕЗ = 120` (216) и флаг `_спал`: два фона — «дремлет» (днём, легко разбудить) и «ночь_спит»; во сне раз в час-четыре сценка-проверка «приоткрыл глаз, посмотрел, закрыл».
12. Всё новое (настроение, доверие, привязанность, долг сна, колоды) — в `снимок()`/`восстановить()` из волны 1.

**ВИДНО:** у дня появляется окраска («сегодня он какой-то тихий»); зов внимания больше не тонет среди спокойных сценок; на один и тот же повод приходят разные исполнения, и на третий тычок он уже сердится; длинные сценки досматриваются; изредка он просто не замечает мелочи.
**ИИ:** нет.

**ПРОВЕРКИ** (`pc/selftest.py`, `test_scenes`):
- зов внимания: при молчащем человеке доля зовущих сценок ≥ 0.8 без константы-костыля;
- колода: за N выборов подряд в группе из 6 ни одна не повторяется, пока не сыграны все;
- sequential: три касания подряд дают три РАЗНЫХ звена по возрастанию;
- «имя» с привыканием-единицей: десять вызовов подряд не снижают вес;
- вероятность отклика: на 1000 мелких поводов сыграно 70-90%; на 100 тревог — 100%;
- «дождаться»: событие не прерывает сценку с `прерывает="дождаться"` и приходит следом;
- сутки модельного времени: сонливость наступает по долгу, а не по часам; гистерезис не даёт мигания «заснул-проснулся» чаще раза в 10 минут.

**МУТАЦИИ:** (а) вернуть `ГЛАВНЫЙ_ФОН_ТЯЖЕЛЕЕ` вместо фильтра; (б) выключить колоду (`режим = "жребий"`); (в) сделать все `вероятность = 1.0` — падает проверка неидеальной отзывчивости.

### 3B. Библиотека: закрыть бедные поводы
**ФАЙЛЫ:** `face/library.py`.

**ДЕЛАТЬ** (только данные, ни строчки логики):
1. **«Говорит» — +20 сценок** к нынешним четырём (800, 802, 805, 807). Это фон, который держится всю минуту ответа, когда на персонажа смотрят дольше всего: разные амплитуды жестикуляции, счёт на пальцах, ладонь ребром, «во-первых… во-вторых», взгляд вверх при подборе слова, пауза с поднятым пальцем. Параметры уже есть: РУКА_Л/РУКА_П, ГОЛОВА_*, БРОВЬ_*.
2. **«Ночь_спит» — +10** к двум (1085, 1089) на восемь часов сна: поёрзал, перевернулся, вздохнул, приоткрыл глаз и проверил, здесь ли человек (с отдыхом порядка часа).
3. **21 повод с одной сценкой** довести до 4: батарея_мало (926), вечер_встреча (1282), делает_считает (905), делает_таймер (886), делает_что_то (910), дождь (931), жара (937), обед (1279), поехали (857), поздно (1286), понедельник (1293), праздник (1296), приехали (860), пятница (1290), снег (934), таймер_кончился (918), таймер_поставлен (1341), услышал_люблю (828), услышал_молодец (821), услышал_смех (824), услышал_спасибо (811). Плюс 15 поводов с двумя — до 4.
4. **Новые поводы волны 2** обеспечить сценками: погладили_голову/тело (5 уровней лестницы, связанных `следом`), ткнули, щекочут, держат, хозяин_пришёл, гость_пришёл, давно_не_виделись, подошёл_вплотную, отошёл_далеко, услышал_да/нет/стоп/прости/спокойной_ночи/как_дела, вышло, отказ, человек_рядом_ночью, покой_вечером, покой_днём, дремлет, тревожно (настороженные: оглядывается, прислушивается, переступает).
5. **Цепочки `следом` — два десятка по одному слову** на уже написанных сценках, ТОЛЬКО внутри одного повода и при сопоставимом отдыхе (звено не проверяется на отдых, 1947-1950; фон сменился — звено оборвётся, 1963-1968): «зевнул украдкой» (429) → «потёр глаз» (353); «принюхался» (385) → «сдул пылинку» (416); «подмигнул» (325) → «большой палец» (520); «считает в уме» (375) → «загибает пальцы» (379); «прислушался» (382) → «ладонь к уху» (397); «сделал вид, что не смотрит» (537) → «отвёл глаза и подглядывает» (477); «шепчет по секрету» (550) → «прикрыл рот ладонью» (554); «наспех причесался» (607) → «приосанился» (604); «козырнул» (611) → «вытянулся» (558).
6. Проверить, что новые сценки начинаются с кадра-замаха (anticipation), а не сразу с пика, и последний кадр не равен точно позе покоя (follow-through: чуть проскочить и вернуться).

**ВИДНО:** минута ответа перестаёт быть четырьмя кивками по кругу; на каждое «спасибо» — не тот же поклон; ночь перестаёт быть 584 кругами «сопит»; жест становится двухтактной шуткой.
**ИИ:** нет (это ручная работа, и она должна остаться ручной).

**ПРОВЕРКИ** (`pc/selftest.py`): ни один повод из `СОБЫТИЯ`+`ФОНЫ` не имеет меньше 4 сценок (число печатать); все `следом` указывают на существующие имена и на тот же повод; ни одного дубля имён; отдых звена не более чем вдвое отличается от родителя; за минуту фона «говорит» — не менее 12 жестов и не менее 10 разных имён.
**МУТАЦИИ:** удалить три сценки из «говорит» — падает проверка покрытия поводов.

### 3C. Тело: фиджеты, вес, упреждение
**ФАЙЛЫ:** `face/character.py`.

**ДЕЛАТЬ:**
1. **5-8 микрофиджетов** своим таймером под сценками: `_блуждает:504` и ветка «стоит» (520) — почесался, переступил, поправил, повёл плечом, потянул шею. Не повторять один и тот же подряд, накладывать друг на друга; таймер — экспоненциальный, а не сетка.
2. **Перенос веса и асимметрия.** `_пустая_поза:139` и «стоит»: медленный перенос веса циклом 4-8 с (сдвиг x на доли процента ширины + наклон) плюс постоянная лёгкая асимметрия плеч/наклона, своя для персонажа (из `зерно_робота`, но НЕ внутри чистых функций — как постоянная, вычисленная в `__init__`).
3. **Упреждение.** `_решить:450` принимает решение и тут же исполняет. Разнести: сначала взгляд и наклон в сторону будущего шага, через 0.25-0.4 с — тронуться (`_шагнуть:486`). `_наклон_к:350` при повороте головы: корпус ОТСТАЁТ на 0.1-0.2 с (это же даёт follow-through бесплатно).
4. **Закон 2/3.** `_скорость_множитель:468` — домножить на κ^(−1/3) от кривизны текущего участка: в повороте скорость падает.
5. **Самодвижение.** `_решить`: изредка остановиться посреди шага, развернуться без повода, ускориться на ровном месте (это признак живого №1 после колебаний траектории).
6. **Очередь целей взгляда** с приоритетом и сроком (человек, источник звука, касание, точка интереса, край экрана) вместо жребия в `_фиксация`; глаза ведут, наклон догоняет.
7. **Веса дыхания/саккад от возбуждения** (`self.возбуждение`, поле из 2A): возбуждён — чаще и мельче, спокоен — реже и глубже.

**ВИДНО:** персонаж не «стоит и ждёт», а коротает время; видно, что у него есть вес и что он собирается сделать, до того как сделал.
**ИИ:** нет.
**ПРОВЕРКИ** (`voice/selftest.py`): за 10 минут покоя сыграно ≥ 5 разных фиджетов, ни один дважды подряд; поза покоя несимметрична (левое и правое плечо различаются постоянно); перед каждым началом шага взгляд уходит в сторону цели не позже чем за 0.2 с; на повороте скорость ниже, чем на прямой, минимум на 20%.
**МУТАЦИИ:** убрать задержку между взглядом и шагом (упреждение) — падает проверка anticipation.

### 3D. Страница: хит-зоны и настроение в теле
**ФАЙЛЫ:** `pc/avatar/index.html`.

**ДЕЛАТЬ:** развести `модель.motion(тап)` (434) по настоящим hit areas (HitAreaHead/HitAreaBody, `hitTest`), приоритеты Live2D (Idle 1 < Normal 2 < Force 3): касание вытесняет покой, но не вытесняет форс; мышь/тач по холсту → те же события, что у робота; `АБСОЛЮТНЫЕ` брать из поля `"абсолютные"` состояния (3A), хардкод убрать; настроение (ярлык + возбуждение) как множитель амплитуды и темпа всех слоёв — Multiply-режим, а не отдельная анимация.
**ВИДНО:** Марка тоже можно потрогать, и голова с животом отвечают по-разному; вечером он весь медленнее.
**ИИ:** нет.
**ПРОВЕРКИ** (`pc/page_selftest.py`): клик по голове и по животу дают разные группы движений; при `возбуждение = 0.2` средняя амплитуда параметров за 30 с ниже, чем при 0.8, минимум в 1.5 раза; список абсолютных совпадает с тем, что прислал сценарист.
**МУТАЦИИ:** вернуть хардкод абсолютных и разойтись с `library.py` — падает проверка совпадения.

### 3E. Издатель: обстановка задаёт базовую линию
**ФАЙЛЫ:** `voice/robot_voice/face.py`.

**ДЕЛАТЬ:** добавить в состояние `"свет"` (средняя яркость кадра камеры) и `"шум"` (уровень микрофона) — базовая линия возбуждения (MiRo): в тёмной тихой комнате персонаж другой, чем в светлой шумной, без единой новой сценки. Плюс «session-only» узнавание: `кто.видел_сегодня` — помнить, что этот человек был в кадре последние N часов, даже не зная имени.
**ВИДНО:** вечером при выключенном свете он сам притихает; «я тебя сегодня уже видел» работает без распознавания лиц.
**ИИ:** нет.
**ПРОВЕРКИ** (`voice/selftest.py`): тёмный кадр + тишина → `свет`/`шум` низкие и стабильные; поля не роняют состояние при отсутствии камеры/микрофона.
**МУТАЦИИ:** не требуется (новые поля, без защиты) — вместо неё проверка на деградацию: камера отвалилась → поля отсутствуют, сценарист работает.

---

## ВОЛНА 4 — САМООБУЧЕНИЕ И ЗАМЫКАНИЕ ПЕТЛИ

### 4A. Сценарист учится на поведении человека
**ФАЙЛЫ:** `face/scenes.py`.

**ДЕЛАТЬ:**
1. **Окно отклика.** Метод `отклик(с, t)` рядом с `_отметить:1838`: окно открывается в момент, когда сценка ОТЫГРАНА (одна сценка = одно наблюдение, а не 30 в секунду), длится 8-12 с. Плюс: задержался в кадре (+0.3), подошёл (+0.4), заговорил/позвал (+1.0), смех (+1.0), касание (+1.0), «молодец»/«спасибо» (+1.0). Минус: ушёл внутри окна (r = 0), отвернулся (−0.3), перебил (−0.5). `r = clip(Σ, 0, 1)`.
2. **Не наказывать за то, чего не видели.** Наблюдение засчитывается ТОЛЬКО если человек был в кадре и взгляд был к роботу хотя бы часть окна; пока робот говорит — не учить вовсе (реакция на речь, а не на жест). Иначе через месяц персонаж выучит «ночью и в пустой комнате всё плохо».
3. **Учить только фон.** Проверка `сц.когда in ФОНЫ` в точке обновления статистики: у событийных сценок (приветствие, тревога, батарея, таймер) α/β не ведутся никогда. Худший исход обучения становится безобидным.
4. **Подключить `learn.Опыт`** в `_вес:1857` последним множителем (обучение множит, привыкание делит — порядок не ломать) и признаки сценки вычислять автоматически в `_с()`/при загрузке: есть ли РУКА_Л/РУКА_П, есть ли текст, max|ГОЛОВА_*|, длительность, зеркальная ли.
5. **Контекст:** ключ `(кто × главный фон × блок суток)`, базовая линия того же контекста, обучение на преимуществе (не на сыром отклике), усадка к родителю.
6. **Потолки поверх обучения** (Goodhart): `ЗОВЁТ_НЕ_ДОЛЬШЕ`/`ЗОВЁТ_НЕ_ЧАЩЕ` остаются жёсткими и обучению не подчиняются; ночью не звать вовсе; не больше N зовущих сценок в час. Обучение НЕ трогает темп, паузы, дыхание и настроение — только выбор сценки из уместных.
7. **Батч:** множители пересчитывать на границе суток (сторож `_час_был` уже есть), а не каждым наблюдением. Первые 3-7 суток — СОБИРАТЬ, но не применять (эффект новизны).
8. **`сводка(сколько=10)`** рядом с `недавние:1530` — человекочитаемые строки «прячется-выглядывает 0.62 при базе 0.31, 14 наблюдений».

**ВИДНО:** через месяц он чаще делает то, на что этот дом отзывался, и реже то, от чего отворачивались; но репертуар не сжимается — ни одна сценка не исчезает.
**ИИ:** нет.

**ПРОВЕРКИ** (`pc/selftest.py`, `test_обучение`):
- симулятор хозяина с профилем: за 14 суток доля любимой группы растёт с 0.33 до ≥0.5; минимальная частота любой сценки ≥ 40% авторской (защита от схлопывания);
- ни одна событийная сценка не получила ни одного наблюдения;
- сценка, сыгранная в пустой комнате, не меняет ни α, ни β;
- один эпизод даёт ровно одно наблюдение (не 60);
- смена вкуса на 15-е сутки подхватывается за ≤ 14 суток;
- за час зова внимания число зовущих сценок не превышает потолок, ночью — ноль (Goodhart-тест на живом прецеденте: 975 сценок/час, 766 стуков).

**МУТАЦИИ:** (а) убрать проверку «человек в кадре» в `отклик`; (б) убрать `сц.когда in ФОНЫ` — обучение поедет на приветствия; (в) снять ε-пол; (г) снять потолок зова.

### 4B. Память: биография
**ФАЙЛЫ:** `face/memory.py`, `face/learn.py`.

**ДЕЛАТЬ:** хранить и состаривать: `часы_жизни`, `доверие`, `привязанность`, `долг_сна`, `люди: {имя: {встреч, секунд_вместе, поглаживаний, последняя_встреча}}`, `распорядок` (48 клеток), α/β по сценкам (295 ≈ 12 КБ), групповые статистики, базовые линии, `зерно`, `праздники`, версия схемы и миграция. Пожизненные счётчики в духе RobotStatsTracker — не «сколько команд выполнил», а «сколько секунд был возбуждён», «сколько его гладили», «сколько раз реагировал на взгляд». Запись раз в минуту и принудительно на выходе (питание рубится рубильником — на выход рассчитывать нельзя).
**ВИДНО:** «он меня знает»: после разлуки встречает иначе, чем после отлучки на кухню; с возрастом становится сдержаннее.
**ИИ:** нет.
**ПРОВЕРКИ:** файл 295 сценок + люди + распорядок < 64 КБ; чтение битого — авторские веса без единой жалобы; миграция версии 1→2 не теряет `часы_жизни`; после «трёх суток отсутствия» счётчики не восстанавливаются как были, а состариваются.
**МУТАЦИИ:** убрать состаривание при загрузке — падает проверка трёх суток.

### 4C. Режиссёр знает этот дом (единственный пункт с ИИ)
**ФАЙЛЫ:** `face/director.py`, `pc/kuzya_pc.py`.

**ДЕЛАТЬ:** в `обстановка()` (60-94) добавить блок «что заходит» (`сценарист.сводка(10)`), кто перед роботом, время с последней встречи, счётчики из `diary.py`/`people.py`. Второй, редкий запрос раз в час-два: «запиши одной фразой, что было» → строка (до 240 знаков) в `memory` с меткой времени, бюджет — как у Amica, около 3000 токенов на всё; сценарист читает эти строки как факты и как текст поверх сценки (`_текст_поверх`). Помнить предел `ИМЁН_В_СПИСКЕ = 40` (40) — новые сценки в срез сами не попадут, порядок в `годные` (1501-1528) уже это учитывает.
**ВИДНО:** одна непредсказуемая уместная реплика в час, опирающаяся на факты этого дома.
**ИИ:** ДА — и только здесь. Без ПК всё остальное продолжает работать.
**ПРОВЕРКИ** (`pc/selftest.py`): при выключенной модели дневник не пишется и ничего не ломается; сводка не длиннее N знаков; выдуманные имена по-прежнему выбрасываются; дневниковая строка не попадает в пузырь дословно (пересказ, не цитата — дословное цитирование читается как вторжение).
**МУТАЦИИ:** разрешить режиссёру имена мимо `ПО_ИМЕНИ` — падает проверка фильтрации.

### 4D. Экран: сохранять, отдавать сигналы, отчитываться
**ФАЙЛЫ:** `face/face.py`.

**ДЕЛАТЬ:** вызывать `сценарист.снимок()` → `memory.писать()` раз в минуту в главном цикле (рядом с отчётом 2278-2284) и принудительно в `finally` (2288-2299) рядом с `кисть.кадр_отказа`; читать при создании (456). Передавать в сценарист сигналы отклика (касание, уход человека, перебивание). Печатать раз в минуту вместе с к/с: сценок, потерянных событий, множитель обучения min/max, бодрость. Подключить на роботе `director.план_по_правилам` (он чистая логика, сети не требует) — это закрывает «на роботе директора нет вовсе».
**ВИДНО:** после перезагрузки персонаж ведёт себя так же, как вчера вечером; в журнале одной строкой видно всё состояние жизни.
**ИИ:** нет.
**ПРОВЕРКИ:** убийство процесса между записями не портит файл; запись не чаще раза в минуту (бюджет «десятая доля ядра»); после рестарта отдыхи и повторы восстановлены.
**МУТАЦИИ:** убрать дросселирование записи — падает проверка бюджета кадров.

### 4E. Сигналы недовольства из голоса
**ФАЙЛЫ:** `voice/robot_voice/wake.py`, `voice/robot_voice/face.py`.

**ДЕЛАТЬ:** перебивание уже распознаётся (`wake.py`, список `ПЕРЕБИВАНИЕ`, ~193) — прокинуть его отдельным ключом `"перебил": номер` в состояние; там же «переспросил» (повтор той же формулировки в пределах 30 с). Отрицательный сигнал почти однозначен, положительный двусмыслен — веса несимметричны, но отрицательное требует подтверждения повторами.
**ВИДНО:** он перестаёт лезть, когда человек занят.
**ИИ:** нет.
**ПРОВЕРКИ:** перебивание порождает ровно один сигнал, не поток; окно отклика закрывается нулём при уходе человека.
**МУТАЦИИ:** не прокидывать перебивание — падает проверка отрицательного сигнала.

### 4F. Измеритель: симулятор хозяина
**ФАЙЛЫ:** `scripts/прогон_жизни.py`.

**ДЕЛАТЬ:** добавить режим `--хозяин профиль.json` (14-60 суток модельного времени): виртуальный человек с предпочтениями, расписанием и сменой вкуса на 15-е сутки; печатает кривые обучения, покрытие репертуара, метрики раздела 5 и итоговую таблицу по четырём осям.
**ИИ:** нет.

---

## 5. КАК ПРОВЕРИТЬ, ЧТО ДОСТИГЛИ 10/10

Приёмка одной командой:
`python3 voice/selftest.py && python3 pc/selftest.py && python3 pc/page_selftest.py && python3 scripts/mutations.py && python3 scripts/прогон_жизни.py --сутки 30 --хозяин docs/профиль.json`

### Живость (сейчас 6, честно 5)
| Что меряем | Чем | 10/10 |
|---|---|---|
| Неподвижность | доля секунд, где 32×32-хэш кадра не менялся ≥2 с, за 30 суток симуляции, по всем состояниям включая «сплю» и «тревога» | 0.000 |
| Периодичность | максимум автокорреляции подскока и взгляда на лагах 1-120 с | < 0.5 |
| Фиксации взгляда | медиана длины фиксации; доля в коридоре 200-600 мс | 250-400 мс; ≥ 60% |
| Контакт глаз | самый долгий взгляд в упор; отводов за минуту присутствия | ≤ 6 с; ≥ 12 |
| Разнообразие часа рядом с человеком | разных имён; доля самого частого; повторов подряд | ≥ 120; ≤ 3%; 0 |
| Минута речи | жестов; разных имён | ≥ 12; ≥ 10 |
| Покрытие поводов | поводов с < 4 сценками | 0 (сейчас 36) |
| Суточная разница | сценок/час в 10:00 против 23:00 | ≥ 1.6 раза |
| Ночь | доля дневных сценок в 02:00 | 0 |
| Касание | медиана задержки отклика; доля откликов, прошедших через режиссёра | 0.2-0.8 с; 0 |
| Отзывчивость | доля отыгранных мелких поводов | 70-90% (не 100) |

### Независимость от ИИ (сейчас 7)
1. Тот же прогон с `--без-пк`: все метрики живости выше проседают ≤ 10%.
2. Список того, что без ПК не работает, состоит ровно из двух пунктов: реплика режиссёра и дневник. Всё остальное — ноль пунктов (сейчас там праздники хозяина и узнавание).
3. Проверка кодом: `face/face.py` не импортирует сеть; `import face` без `voice/` и без интернета поднимает лицо и даёт ≥ 100 сценок в час.
4. Убитый `ollama` на сутки: в журнале нет ни одной ошибки, влияющей на кадр; план собирается правилами (доля планов «по правилам» = 100%).
5. Робот без ПК в день рождения хозяина поздравляет (сценка «праздник» сыграна ровно один раз за сутки).

### Самообучение (сейчас 2)
1. 30 суток с виртуальным хозяином: доля групп-любимцев 0.33 → ≥ 0.50; отклик (средняя награда) вырос ≥ 25% против контрольного прогона на авторских весах.
2. Репертуар не схлопнулся: минимальная частота любой фоновой сценки ≥ 40% её авторской доли; число уникальных имён за сутки не упало.
3. Событийные сценки: ровно 0 наблюдений, ровно 0 изменённых весов.
4. Смена вкуса на 15-е сутки: возврат к новому оптимуму за ≤ 14 суток (забывание работает).
5. Наблюдений в сутки: 20-60, все — при человеке в кадре; наблюдений при пустой комнате 0.
6. Распорядок: после 14 суток предсказание «человек придёт в ближайший час» имеет точность ≥ 0.7 на профиле с регулярным расписанием и не даёт ложных «ждёт» ночью.
7. Перезапуск в середине прогона: кривые обучения не рвутся (файл 12-64 КБ, состаривание применено).

### Надёжность живости (сейчас 5)
1. `scripts/mutations.py` — 100% «поймана» по всем новым целям (их в плане 25+), ноль «мимо» и ноль «устарела».
2. Шесть инсценированных поломок, каждая — с картинкой и журналом: детектор тел молчит; ПК выключен; страница ПК застыла с исключением; часы не сверены; файл памяти битый; служба лица перезапущена посреди сценки. Требование: во всех шести случаях (а) картинка на экране продолжает меняться (сторож не срабатывает), (б) в журнале есть ровно одна строка с причиной и рецептом, (в) метрики живости из первой таблицы не хуже 70% от нормы. Сейчас случай №1 даёт 2/10 молча.
3. Пиксельный сторож ловит искусственную заморозку кадра за ≤ 6 с — на роботе и на ПК.
4. Сценарист не выдумывает: после часа простоя под потоком с ПК — 0 событий в первом кадре после пропуска.
5. Ни одного потерянного жеста: счётчик `_потеряно` за сутки при нормальной работе ≤ 5; счётчик «не доехало до экрана» = 0.
6. ast-проверка дублей определений — чисто по всем модулям лица.
7. `systemd`: оба юнита с `StartLimitIntervalSec=0`.